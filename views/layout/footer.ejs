            </main>
            <footer class="border-top py-3 px-4 bg-white text-muted small mt-auto">
                <div class="d-flex flex-column flex-sm-row justify-content-between align-items-center gap-2">
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge-dot bg-success"></span>
                        <span><strong>Gestion Scolaire RDC</strong> &bull; Système Haute Performance</span>
                    </div>
                    <div class="d-flex gap-3 text-secondary" style="font-size: 0.8rem;">
                        <span>Session active : <strong><%= user.role %></strong></span>
                        <span>&bull;</span>
                        <span>Version 3.0.0 (Figma Studio Edition)</span>
                    </div>
                </div>
            </footer>
        </div>
    </div>

    <!-- Floating Quick Dock (Bottom Right) -->
    <div class="floating-quick-dock d-none d-md-flex">
        <a href="/eleves/nouveau" class="floating-dock-btn" title="Nouvel Élève"><i class="fas fa-user-plus"></i></a>
        <a href="/finance/paiements/nouveau" class="floating-dock-btn" title="Nouveau Paiement"><i class="fas fa-receipt"></i></a>
        <a href="/presences/pointage" class="floating-dock-btn" title="Pointage Présence"><i class="fas fa-fingerprint"></i></a>
        <button class="floating-dock-btn" id="openCommandPaletteBtn" title="Recherche Globale (Ctrl+K)"><i class="fas fa-search"></i></button>
    </div>

    <!-- Command Palette (Ctrl+K / ⌘K) -->
    <div class="figma-cmd-modal" id="commandPaletteModal">
        <div class="figma-cmd-card">
            <div class="figma-cmd-search-header">
                <i class="fas fa-search text-primary fs-5"></i>
                <input type="text" class="figma-cmd-search-input" id="cmdSearchInput" placeholder="Rechercher élève, matricule, classe, reçu..." autocomplete="off">
                <button class="btn btn-sm btn-light rounded-pill p-1 px-2 text-muted" id="closeCmdPalette">Esc</button>
            </div>
            <div class="figma-cmd-results" id="cmdResultsList">
                <div class="px-3 py-2 small text-muted text-uppercase fw-bold" style="font-size: 0.68rem;">Navigation Rapide</div>
                <a href="/eleves" class="figma-cmd-item">
                    <div class="figma-cmd-icon text-primary"><i class="fas fa-user-graduate"></i></div>
                    <div class="flex-grow-1">
                        <div class="fw-semibold">Annuaire des Élèves</div>
                        <div class="small text-muted">Consulter tous les dossiers d'inscription</div>
                    </div>
                    <span class="badge bg-light text-muted border font-mono">/eleves</span>
                </a>
                <a href="/finance" class="figma-cmd-item">
                    <div class="figma-cmd-icon text-warning"><i class="fas fa-wallet"></i></div>
                    <div class="flex-grow-1">
                        <div class="fw-semibold">Trésorerie & Caisse</div>
                        <div class="small text-muted">Paiements de frais scolaires et dépenses</div>
                    </div>
                    <span class="badge bg-light text-muted border font-mono">/finance</span>
                </a>
                <a href="/presences/pointage" class="figma-cmd-item">
                    <div class="figma-cmd-icon text-success"><i class="fas fa-fingerprint"></i></div>
                    <div class="flex-grow-1">
                        <div class="fw-semibold">Pointage des Présences</div>
                        <div class="small text-muted">Feuille de présence quotidienne</div>
                    </div>
                    <span class="badge bg-light text-muted border font-mono">/presences</span>
                </a>
                <a href="/resultats" class="figma-cmd-item">
                    <div class="figma-cmd-icon text-info"><i class="fas fa-file-invoice"></i></div>
                    <div class="flex-grow-1">
                        <div class="fw-semibold">Palmarès & Bulletins</div>
                        <div class="small text-muted">Impression des bulletins RDC et délibérations</div>
                    </div>
                    <span class="badge bg-light text-muted border font-mono">/resultats</span>
                </a>
            </div>
        </div>
    </div>

    <!-- Scripts CDN -->
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/static/js/main.js"></script>
    
    <script>
        AOS.init({ duration: 500, once: true });
        
        // Mobile Sidebar Toggle
        const sidebarToggle = document.getElementById('sidebarToggle');
        if (sidebarToggle) {
            sidebarToggle.addEventListener('click', function() {
                document.getElementById('sidebar-wrapper').classList.toggle('show');
            });
        }

        // Command Palette Logic
        const cmdModal = document.getElementById('commandPaletteModal');
        const cmdInput = document.getElementById('cmdSearchInput');
        const openCmdBtn1 = document.getElementById('openCommandPalette');
        const openCmdBtn2 = document.getElementById('openCommandPaletteBtn');
        const closeCmdBtn = document.getElementById('closeCmdPalette');

        function openPalette() {
            if (cmdModal) {
                cmdModal.classList.add('active');
                if (cmdInput) {
                    cmdInput.value = '';
                    cmdInput.focus();
                }
            }
        }

        function closePalette() {
            if (cmdModal) cmdModal.classList.remove('active');
        }

        if (openCmdBtn1) openCmdBtn1.addEventListener('click', openPalette);
        if (openCmdBtn2) openCmdBtn2.addEventListener('click', openPalette);
        if (closeCmdBtn) closeCmdBtn.addEventListener('click', closePalette);

        // Keyboard Shortcut Ctrl+K / Cmd+K
        document.addEventListener('keydown', function(e) {
            if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                e.preventDefault();
                if (cmdModal && cmdModal.classList.contains('active')) {
                    closePalette();
                } else {
                    openPalette();
                }
            }
            if (e.key === 'Escape' && cmdModal && cmdModal.classList.contains('active')) {
                closePalette();
            }
        });

        // Close when clicking backdrop
        if (cmdModal) {
            cmdModal.addEventListener('click', function(e) {
                if (e.target === cmdModal) closePalette();
            });
        }
    </script>
    <%- typeof extraJs !== 'undefined' ? extraJs : '' %>
</body>
</html>
